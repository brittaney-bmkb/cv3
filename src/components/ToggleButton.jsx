import { Fab } from "@mui/material"

const ToggleButton = () => {

    return(
        <Fab color="primary" aria-label="list">
            <calcite-icon icon="map" />
        </Fab>
    )
}

export default ToggleButton