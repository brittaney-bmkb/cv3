import { CalciteListItem } from "@esri/calcite-components-react"

const ListItem = ({label}) => {
    return(
        <CalciteListItem label={label}>
        </CalciteListItem>
    )
}

export default ListItem